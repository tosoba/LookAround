<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="place" type="com.example.there.aroundmenow.model.UISimplePlace"/>
        <variable name="onInfoBtnClickListener" type="android.view.View.OnClickListener"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="5dp">

        <TextView android:id="@+id/simple_place_name_text_view"
                  android:layout_width="0dp" android:layout_height="wrap_content" android:text="@{place.name}"
                  android:textSize="16sp" android:textStyle="bold"
                  app:layout_constraintTop_toTopOf="parent"
                  app:layout_constraintLeft_toLeftOf="parent"
                  app:layout_constraintLeft_toRightOf="@id/simple_place_info_image_button"/>

        <TextView android:id="@+id/simple_place_distance_text_view"
                  android:layout_width="0dp" android:layout_height="wrap_content"
                  app:layout_constraintTop_toBottomOf="@id/simple_place_name_text_view"
                  app:layout_constraintLeft_toLeftOf="parent"
                  app:layout_constraintRight_toLeftOf="@id/simple_place_info_image_button"
                  android:textSize="14sp" android:text="@{place.formattedDistanceFromUser}"/>

        <ImageButton android:id="@+id/simple_place_info_image_button"
                     android:layout_width="wrap_content" android:layout_height="wrap_content"
                     app:layout_constraintRight_toRightOf="parent"
                     app:layout_constraintTop_toTopOf="parent"
                     app:layout_constraintBottom_toBottomOf="parent"
                     android:src="@drawable/info"
                     android:padding="3dp"
                     android:layout_margin="3dp"
                     android:background="@color/colorPrimary"
                     android:onClick="@{onInfoBtnClickListener}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>